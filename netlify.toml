[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# Next.js rewrites - handled automatically by Netlify
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/next"
  status = 200

